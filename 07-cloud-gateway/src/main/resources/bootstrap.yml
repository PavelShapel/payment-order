server:
  port: 9003
spring:
  application:
    name: cloud-gateway
  cloud:
    consul:
      host: consul
      port: 8500
      discovery:
        locator:
          enabled: true
        register: false
management:
  endpoints:
    web:
      exposure:
        include: "*"