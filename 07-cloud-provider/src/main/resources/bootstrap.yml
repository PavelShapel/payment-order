server:
  port: 9001
spring:
  zipkin:
    base-url: zipkin:9411
  application:
    name: cloud-provider
  cloud:
    consul:
      host: consul
      port: 8500
      config:
        enabled: true
      discovery:
        instance-id: ${spring.application.name}:${random.int[1,999999]}