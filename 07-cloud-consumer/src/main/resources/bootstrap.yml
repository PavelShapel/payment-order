server:
  port: 9002
spring:
  application:
    name: cloud-consumer
  cloud:
    consul:
      host: consul
      port: 8500
      config:
        enabled: true
      discovery:
        instance-id: ${spring.application.name}:${random.int[1,999999]}